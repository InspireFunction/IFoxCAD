我还是希望一个节点一个节点回滚,这样DAG逻辑清晰.
只是我们节点内不再是只有一个动作了,而是一个动作链,每次触发修改的时候去找当前节点,看看是否能够加入.

能加入:
1,当前节点是<有命令>,当前还没有闭合,直接加入.
2,当前节点是<无命令>,查找动作链条末尾是不是当前图元id,如果是就动作加入到链条上,实现软合并.

不能加入:
就是<无命令>,而且动作链条末尾不是相同图元id,就创建一个自己的节点.